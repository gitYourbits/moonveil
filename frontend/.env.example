# Finagen Portal Environment Configuration

# API Configuration
VITE_API_BASE_URL=http://localhost:8000

# Development Settings
# VITE_ENABLE_DEVTOOLS=true

# Note: All environment variables used in the client must be prefixed with VITE_
# This is a Vite requirement for security - only VITE_ prefixed vars are exposed to the browser